#!/usr/bin/env python3
print("✅ Models ready - using HF auto-download and local stubs")
print("ESM-2 downloads automatically on first use")
print("Local GNN uses RDKit fingerprints (no external download needed)")
print("Run: poetry run python -c 'from backend.app.ml.enzyme_predictor import *'")

